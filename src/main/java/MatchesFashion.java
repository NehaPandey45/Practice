/**
 * Create a Java API for filtering a collection of products.
 * A product can be implemented as a Java bean or POJO with standard fields like name, category, price, etc.
 * The solution should include tests.
 *
 * 1. Specific criteria
 * Return all products where name equals a given value.
 * E.g. name = "wool jacket"
 *
 * 2. Dynamic criteria
 * Return all products where specified attribute equals a given value.
 * E.g. field("category") = "trainers"
 *
 * 3. Operators
 * Return all products where multiple criteria can be combined with operators
 * E.g. field("name") = "wool jacket" AND field("category") = "trainers"
 */

public class MatchesFashion {

}
